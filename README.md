# tRPC, Astro, Solid.js, Better-auth, drizzle, tanstack-query

POC of SSR solid.js islands with new tRPC adapter for solid and tanstack-query

# Setup

```shell
cd @trpc-tanstack-solid-query
bun i && bun run build
cd ..
bun i
bun dev
```
